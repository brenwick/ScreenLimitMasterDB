import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AppPrepScreen = () => {
  return (
    <View>
      <Text>User's List of Apps</Text>
      <Text>
        Main use of this screen is to set up or update which apps the user
        prefers our program to watch.
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AppPrepScreen;
